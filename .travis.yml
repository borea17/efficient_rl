dist: bionic

language: python

python:
    - "3.6"

install:
    - pip install gym networkx matplotlib pandas prettytable
    - python setup.py install

jobs:
    include:
        - stage: test
          name: "Unit Tests"
          script:
              - cd test
              - python -m unittest
        - stage: reproducible research
          name: "Reproduce disseration results"
          script:
              - cd efficient_rl
              - python disseration_script.py
